import { FaSpinner } from 'react-icons/fa';
import styled from 'styled-components';
import { setAnimationSpinner } from './style';

export const Spinner = styled(FaSpinner)`
  ${setAnimationSpinner};
`;
